<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ibmro.ishqa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".onboarding.OnboardingActivity" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profile.ProfileActivity" />
        <activity android:name=".selectTags.TagActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".sectionItemsList.SectionItemListActivity" />
        <activity android:name=".question.questionList.QuestionListActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".candidate.candidateItem.CandidateActivity" />
        <activity android:name=".question.questionItem.QuestionActivity" />
        <activity android:name=".interview.interviewsList.InterviewsListActivity" />
        <activity android:name=".candidate.candidateList.CandidateListActivity" />
        <activity android:name=".interview.interviewItem.InterviewActivity" />
        <activity android:name=".selectQuestion.SelectQuestionActivity" />
        <activity android:name=".interviewer.interviewersList.SelectInterviewersActivity" />
        <activity android:name=".interview.playInterview.PlayInterviewActivity" />
        <activity android:name=".interviewer.interviewerItem.InterviewerActivity" />
        <activity android:name=".candidate.candidateItem.WebViewActivity" />
        <activity android:name=".sign.login.LoginActivity"></activity>
    </application>

</manifest>